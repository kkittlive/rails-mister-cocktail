<h1>Add an ingredient to this cocktail</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Add the ingredient", class: "btn btn-primary" %>
<% end %>

<%= link_to 'Return to full cocktail list', cocktails_path %>
